

// const express = require('express');
// const router = express.Router();
// const userController = require('../controllers/usersController'); // Ensure this path is correct

// // Define routes
// router.get('/', userController.getAllUsers);
// router.post('/api/users', userController.addUser);
// router.put('/api/users/:id', userController.updateUser);
// router.delete('/api/users/:id', userController.deleteUser);
// router.put('/api/users/:id/activate', userController.activateUser);
// router.put('/api/users/:id/deactivate', userController.deactivateUser);

// module.exports = router;


const express = require('express');
const router = express.Router();
const userController = require('../controllers/usersController');

// Get all users
router.get('/users', userController.getAllUsers);

// Add a new user
router.post('/users', userController.addUser);

// Update a user
router.put('/users/:id', userController.updateUser);

// Soft delete a user
router.delete('/users/:id', userController.deleteUser);

// Activate a user
router.post('/users/:id/activate', userController.activateUser);

// Deactivate a user
router.post('/users/:id/deactivate', userController.deactivateUser);

module.exports = router;
